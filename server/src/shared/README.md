# shared 目录说明

该目录用于存放跨模块共享能力，避免重复实现与循环依赖。

## 当前内容

- `db/`：数据库相关能力（Drizzle Schema、SQLite 初始化、种子数据、基础数据访问）

## 适用范围

- 可被多个业务模块复用的能力（如数据库、日志、错误模型、通用工具）。
- 与具体业务模块无强耦合的基础设施封装。

## 维护约定

- 禁止在 `shared` 中引入具体业务模块实现。
- 共享能力变更需关注对全部模块的兼容性影响。
