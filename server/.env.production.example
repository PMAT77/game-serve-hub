# 复制为 server/.env.production 后使用（本地构建验证或自定义镜像）：
# cp server/.env.production.example server/.env.production

SERVER_HOST=0.0.0.0
SERVER_PORT=9527
DB_PATH=./data/game-server-hub.sqlite
SERVER_LOG_DIR=./logs
LOG_LEVEL=info
